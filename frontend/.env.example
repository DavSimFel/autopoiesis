# ============================================================
# Autopoiesis PWA — Environment Variables
# Copy to .env and fill in your values.
# ============================================================

# MCP API server (CF Tunnel → localhost:8420)
# The proxy route at /mcp/* forwards to this URL.
MCP_API_URL=https://autopoiesis-api.feldhofer.cc

# ---- Client-side (VITE_ prefix = exposed to browser) --------

# Base URL for MCP tool calls from the browser.
# When running behind the SvelteKit proxy, use same-origin /mcp.
# For direct calls (bypassing proxy), use the API URL directly.
VITE_MCP_BASE_URL=/mcp

# SSE endpoint for server-sent push events.
# Same-origin via proxy — avoids CORS entirely.
VITE_SSE_ENDPOINT=/mcp/sse
